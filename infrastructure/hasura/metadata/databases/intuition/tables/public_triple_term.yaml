table:
  name: triple_term
  schema: public
configuration:
  column_config: {}
  custom_column_names: {}
  custom_root_fields:
    select: triple_terms
    select_by_pk: triple_term
object_relationships:
  - name: counter_term
    using:
      manual_configuration:
        column_mapping:
          counter_term_id: id
        insertion_order: null
        remote_table:
          name: term
          schema: public
  - name: term
    using:
      manual_configuration:
        column_mapping:
          term_id: id
        insertion_order: null
        remote_table:
          name: term
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - counter_term_id
        - term_id
        - total_assets
        - total_market_cap
        - total_position_count
        - updated_at
      filter: {}
      limit: 250
    comment: ""
