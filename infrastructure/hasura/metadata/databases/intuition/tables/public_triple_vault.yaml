table:
  name: triple_vault
  schema: public
configuration:
  column_config: {}
  custom_column_names: {}
  custom_root_fields:
    select: triple_vaults
    select_by_pk: triple_vault
object_relationships:
  - name: counter_term
    using:
      manual_configuration:
        column_mapping:
          counter_term_id: id
        insertion_order: null
        remote_table:
          name: term
          schema: public
  - name: term
    using:
      manual_configuration:
        column_mapping:
          term_id: id
        insertion_order: null
        remote_table:
          name: term
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - log_index
        - position_count
        - block_number
        - counter_term_id
        - curve_id
        - market_cap
        - term_id
        - total_assets
        - total_shares
        - updated_at
      filter: {}
      limit: 100
    comment: ""
