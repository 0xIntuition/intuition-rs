query GetPredicateObjects {
  predicate_objects(
    order_by: [{position_count: desc}, {triple_count: desc}]
  ) {
    position_count
    triple_count
    object {
      label
      data
      term_id
    }
    predicate {
      label
      term_id
    }
  }
}

