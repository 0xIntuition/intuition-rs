query GetPredicateObjects {
  predicate_objects(
    order_by: [{triple_count: desc}]
  ) {
    triple_count
    object {
      label
      data
      term_id
    }
    predicate {
      label
      term_id
    }
  }
}

