# API key for authenticating requests to the health-check endpoint
API_KEY=your-secret-api-key-here

# Port for the server to listen on (default: 3000)
PORT=3000

# Rust log level (default: info)
RUST_LOG=info
