apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vectorapps
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - list:
        elements:
          - cluster: prod-eks
            server: 'https://kubernetes.default.svc'
            revision: luis/eng-6546-vector-db-support
            namespace: default
  template:
    metadata:
      name: '{{ cluster }}-vectorapps-{{namespace}}'
      annotations:
        argocd.argoproj.io/manifest-generate-paths: '.;/devops/argocd'
    spec:
      project: prod
      source:
        repoURL: ssh://git@github.com/0xintuition/intuition-rs.git
        path: devops/argocd/vectorapps
        targetRevision: '{{ revision }}'
        kustomize: {}
      destination:
        server: '{{ server }}'
        namespace: '{{ namespace }}'
