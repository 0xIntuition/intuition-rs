apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: db-main-creds
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - list:
        elements:
          - cluster: prod-eks
            server: 'https://kubernetes.default.svc'
            revision: luis/eng-6546-vector-db-support
            namespace: prod-base-sepolia-v-1-5-vector
          - cluster: prod-eks
            server: 'https://kubernetes.default.svc'
            revision: next
            namespace: prod-base-mainnet-v-1-5-vector
  template:
    metadata:
      name: '{{ cluster }}-db-main-creds-{{namespace}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/0xintuition/intuition-rs.git
        path: 'devops/argocd/manifests/db-main-creds'
        targetRevision: '{{ revision }}'
      destination:
        server: '{{ server }}'
        namespace: '{{ namespace }}'
      syncPolicy:
        automated:
          prune: false
          selfHeal: false
        syncOptions:
        - CreateNamespace=true
      ignoreDifferences:
      - group: ""
        kind: Secret
        name: main-db-creds
        jsonPointers:
          - /data
