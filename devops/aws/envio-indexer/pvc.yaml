apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: envio-indexer-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2-immediate
  resources:
    requests:
      storage: 1Gi 