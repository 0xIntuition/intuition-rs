apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: decoded-logs
  name: decoded-logs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 1Gi
