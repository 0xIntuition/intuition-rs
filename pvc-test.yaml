apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: decoded-logs
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 1Gi 