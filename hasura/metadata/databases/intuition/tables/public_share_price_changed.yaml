table:
  name: share_price_changed
  schema: public
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: share_price_changes
  custom_root_fields:
    select_by_pk: share_price_changed
    select_by_term_id: share_price_changed_by_term_id
    select_by_term_curve_id: share_price_changed_by_term_curve_id
object_relationships:
  - name: term
    using:
      foreign_key_constraint_on: term_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - term_id
        - share_price
        - total_assets
        - total_shares
        - block_number
        - block_timestamp
        - transaction_hash
        - updated_at
      filter: {}
      limit: 100
      allow_aggregations: true
    comment: ""
