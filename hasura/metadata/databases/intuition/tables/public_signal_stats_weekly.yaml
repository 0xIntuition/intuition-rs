table:
  name: signal_stats_weekly
  schema: public
object_relationships:
  - name: term
    using:
      manual_configuration:
        column_mapping:
          term_id: id
        insertion_order: null
        remote_table:
          name: term
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - count_weekly
        - curve_id
        - term_id
        - volume_weekly
        - bucket_weekly
      filter: {}
      limit: 1000
    comment: ""
