table:
  name: stats
  schema: public
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: stats
  custom_root_fields:
    select_by_pk: stat
  query_configuration:
    default_limit: 250
    max_limit: 250
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - total_accounts
        - total_atoms
        - total_positions
        - total_signals
        - total_triples
        - contract_balance
        - total_fees
      filter: {}
      limit: 98
      allow_aggregations: true
    comment: ""
