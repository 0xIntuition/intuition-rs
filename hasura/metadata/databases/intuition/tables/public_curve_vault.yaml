table:
  name: curve_vault
  schema: public
object_relationships:
  - name: atom
    using:
      manual_configuration:
        column_mapping:
          atom_id: id
        insertion_order: null
        remote_table:
          name: atom
          schema: public
  - name: triple
    using:
      manual_configuration:
        column_mapping:
          triple_id: id
        insertion_order: null
        remote_table:
          name: triple
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - atom_id
        - triple_id
        - curve_number
        - total_shares
        - current_share_price
        - position_count
      filter: {}
