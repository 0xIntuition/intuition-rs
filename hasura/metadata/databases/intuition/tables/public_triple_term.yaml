table:
  name: triple_term
  schema: public
configuration:
  column_config: {}
  custom_column_names: {}
  custom_root_fields:
    select: triple_terms
    select_by_pk: triple_term
object_relationships:
  - name: counter_term
    using:
      foreign_key_constraint_on: counter_term_id
  - name: term
    using:
      foreign_key_constraint_on: term_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - counter_term_id
        - term_id
        - total_assets
        - total_market_cap
        - total_position_count
        - updated_at
      filter: {}
      limit: 250
    comment: ""
